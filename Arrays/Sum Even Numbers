function sumEvenNumbers(input) {
    let sum = 0;
    for (let i = 0; i <= input.length - 1; i++) {
        let x = parseInt(input[i]);
        if (x % 2 === 0) {
            sum += x;
        }
    }
    console.log(sum);
}
